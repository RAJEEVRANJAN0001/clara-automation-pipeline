# ─── Core (no install needed — stdlib only) ────────────────────────────────────
# The base pipeline uses Python standard library only (zero external deps).

# ─── Optional: Audio Transcription ────────────────────────────────────────────
# Uncomment to enable Whisper-based audio transcription:
# openai-whisper>=20230918

# ─── Optional: Enhanced HTML Reports ──────────────────────────────────────────
# Uncomment for richer diff reports:
# jinja2>=3.1.2

# ─── Optional: LLM-based Extraction (future upgrade path) ─────────────────────
# Uncomment to swap rule-based extraction for LLM:
# openai>=1.0.0
# anthropic>=0.20.0

# ─── Development / Testing ─────────────────────────────────────────────────────
# pytest>=7.4.0
# pytest-cov>=4.1.0
